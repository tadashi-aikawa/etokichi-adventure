# 開発ルール

## マップ用キャラクター素材

- 生成AIが出力した3列×4行画像を、そのまま`assets/map-characters/<ID>/source/walk.png`へ置かない。
- 各ポーズがセル境界をまたぐことと、歩行3コマの中心軸・足元がずれることを目視だけで判定しない。
- 素材の追加・作り直しでは、必ず`pnpm map-character:normalize -- <入力PNG> <原本PNG> <配信用WebP>`で各ポーズを共通セルへ再配置する。
- コミット前に`pnpm map-character:validate`を実行し、全キャラクターで境界余白・中心軸・足元の検査を通す。
- 正規化後も、実画面で上下左右それぞれの停止・歩行を確認する。欠け、隣セルの混入、進行方向と直交する揺れがあれば採用しない。
